[project]
name = "lab-sys-admin"
version = "0.1.0"
description = "Ansible-based system administration for MISL Lab servers"
requires-python = ">=3.12"
dependencies = [
    "ansible-core>=2.17",
    "jmespath>=1.0.0",
    "passlib>=1.7.4",
]

[tool.uv]
dev-dependencies = [
    "ansible-lint",
]
